#include <stdio.h>
#include <string.h>
int main()
{
    char s[101];
    scanf("%s", s);

    // step 1
    // int size = strlen(s);

    // step 2
    printf("%d", strlen(s));

    // int count = 0;
    // for (int i = 0; s[i] != 0; i++)
    // {

    //     count++;
    // }

    // printf("%d", count);

    return 0;
}